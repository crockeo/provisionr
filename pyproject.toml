[project]
name = "provisionr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "alembic",
  "fastapi",
  "nicegui",
  "pydantic",
  "uvicorn",
]
